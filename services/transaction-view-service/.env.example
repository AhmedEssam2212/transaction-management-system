# ============================================================================
# TRANSACTION VIEW SERVICE - ENVIRONMENT CONFIGURATION (EXAMPLE)
# ============================================================================
#
# SETUP INSTRUCTIONS:
# 1. Copy this file to .env:
#    cp .env.example .env
#
# 2. Generate secure secrets for production:
#    node scripts/generate-secrets.js
#
# 3. Update the values below with your actual configuration
#
# SECURITY NOTES:
# - NEVER commit .env files to version control
# - Use different secrets for each environment
# - Rotate secrets regularly in production
# - Store production secrets in a secure vault
#
# ============================================================================

# Server Configuration
PORT=3000
NODE_ENV=development

# Database Configuration
# Format: postgresql://[user]:[password]@[host]:[port]/[database]?[params]
#
# For local development:
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/transaction_db?sslmode=disable&connect_timeout=10"
#
# For production (example with SSL):
# DATABASE_URL="postgresql://user:password@prod-host:5432/transaction_db?sslmode=require&connect_timeout=10"

# JWT Configuration
# REQUIRED in production! Generate using: node scripts/generate-secrets.js
# For development, leave commented (auto-generated for convenience)
#
# JWT_SECRET=<GENERATE_SECURE_SECRET_HERE>
JWT_EXPIRES_IN=24h

# NATS Configuration
NATS_URL=nats://localhost:4222
NATS_CLUSTER_ID=transaction-cluster

# Service Configuration
SERVICE_NAME=transaction-view-service

# CORS Configuration
# Comma-separated list of allowed origins for production
# For development, defaults to localhost:3000,localhost:3001
# For production, specify your actual domains:
# ALLOWED_ORIGINS=https://yourdomain.com,https://app.yourdomain.com
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001

